package com.debtlens.backend.controller;

public class UserController {
}
/*Handles user-related API operations.

Possible responsibilities:

GET /api/users/me
GET /api/users/{id}
PUT /api/users/{id}

For example:

React
 ↓
GET /users/me
 ↓
UserController
 ↓
UserService

It can obtain the authenticated user's Auth0 identity from Spring Security.*/
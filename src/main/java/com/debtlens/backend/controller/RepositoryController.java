package com.debtlens.backend.controller;

public class RepositoryController {
}
 /*RepositoryController.java

Handles repository-related operations.

For example:

GET /companies/{companyId}/repositories
GET /repositories/{repositoryId}
GET /repositories/{repositoryId}/members

It can also handle repository selection/assignment operations.

The controller delegates to:

RepositoryService*/